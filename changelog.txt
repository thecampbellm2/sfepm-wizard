- Save dialog now pre-fills to H:\My Drive\Jobs\[YYYY]\[MM MMM]\[Job Name]\ based on job name and current date
- Folders are created automatically if they don't exist
- Falls back to standard folder picker silently if H: drive is unavailable
- Invalid Windows filename characters stripped from job name when building path
